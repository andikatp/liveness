# passive_liveness_example

An example application demonstrating how to initialize and use the `passive_liveness` package for camera stream and static photo file liveness detection in Flutter.

## Running the example

```bash
cd example
flutter run
```
