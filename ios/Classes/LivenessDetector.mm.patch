--- LivenessDetector.mm	2026-07-21
+++ LivenessDetector.mm	2026-07-21
@@ -218,6 +218,9 @@
     bottom = (int)[box[@"bottom"] integerValue];
   }
 
+  // Debug: Print raw box from dart
+  NSLog(@"[LivenessDetector] RAW BOX FROM DART: L=%d T=%d R=%d B=%d for image W=%d H=%d (orientation=%d)", left, top, right, bottom, width, height, orientation);
+
   // --- Clamp coordinates to image boundaries ---
   left = std::max(0, left);
   top = std::max(0, top);
